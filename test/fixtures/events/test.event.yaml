pages:
  - state:
      charset: oldMan
      direction: left
      blocking: true
      trigger: Click
    actions:
      - type: message
        message:
          text: 'Hello there! You are on {{ map.id }}'
      - type: message
        message:
          text: 'How do you feel with this?'
      - type: message
        message:
          text: 'This is awesome!?'
